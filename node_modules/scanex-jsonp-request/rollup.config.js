import babel from 'rollup-plugin-babel';
import pkg from './package.json';

export default {
    input: 'index.js',		
    output: { 
        file: pkg.module,
        format: 'cjs',
    },
    plugins: [
        babel(),
    ],
};